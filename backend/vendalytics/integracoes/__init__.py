"""
integracoes/ — conectores de CRM (spec §2.1 A6).

Mesmo padrão do resto do projeto: contrato em `base.py`
(`CRMConnector`), implementação de referência em `csv_connector.py`
(`CSVCRMConnector`) que funciona de verdade hoje, sem depender de
credencial de nenhum provedor externo para ser validada.
"""
